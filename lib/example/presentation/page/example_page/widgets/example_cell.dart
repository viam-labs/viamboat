import 'package:flutter/material.dart';

class ExampleCell extends StatelessWidget {
  const ExampleCell({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
