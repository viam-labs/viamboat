import 'package:flutter/cupertino.dart';

class ExampleCommonWidget extends StatelessWidget {
  const ExampleCommonWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
