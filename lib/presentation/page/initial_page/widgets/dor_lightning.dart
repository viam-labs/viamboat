import 'package:flutter/widgets.dart';
import 'package:viam_marine/style/images.dart';

class DorLightning extends StatelessWidget {
  const DorLightning({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(Illustrations.lightning);
  }
}
