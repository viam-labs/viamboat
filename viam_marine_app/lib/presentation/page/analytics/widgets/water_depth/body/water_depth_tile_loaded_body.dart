import 'package:flutter/material.dart';

class WaterDepthTileLoadedBody extends StatelessWidget {
  const WaterDepthTileLoadedBody({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
